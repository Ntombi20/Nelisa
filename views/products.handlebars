<div class="table-responsive">
  <h1>Products</h1>
  <h3><a href="/products/add">Add</a></h3>
    <table class="table table-bordered myTable">
      <thead>
          <tr>
              <th>Product</th>
              <th>Category</th>
          </tr>
      </thead>
        {{#products}}
            <tbody>
                <tr>
                    <td>{{product}}</td>
                    <td>{{categoryName}}</td>
                    <td><a href="/products/edit/{{product_id}}">Edit</a></td>
                    <td><a href="/products/delete/{{product_id}}">Delete</a></td>
                </tr>
            </tbody>
            {{/products}}
    </table>
</div>
